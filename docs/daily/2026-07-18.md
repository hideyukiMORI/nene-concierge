# 2026-07-18

C1（#178/#179）の残穴として査定が特定した **suppressions 増量防止ゲートの欠落** を ratchet で塞ぎ、その作業中に発見した **コミット済み admin 生成物の乖離** を hub 裁定を受けて同期した。指揮は統合リナ（hub）・Issue 駆動・全 PR CI 緑で self-merge（hub 事前許可）。フリートではこの日 ayane.co.jp が自社スタックで刷新ローンチ〔伝聞: hub 同報〕。

---

## suppressions 増量防止 ratchet（#182 / PR #183・merged: 0644a20）
- C1 で凍結した `eslint-suppressions.json` は「新規違反は error になるが、**凍結ファイル自体の増量を防ぐ機械検証が無い**」状態だった（レビュー運用依存・フリート査定の指摘〔伝聞: hub〕）。実際、C1 時点 518 件/32 files → 本日 522 件/33 files に増えていた〔実測: PR #179 本文と現ファイルの比較〕。
- `frontend/scripts/eslint-suppressions-ratchet.mjs`（依存ゼロ Node）を新設: `origin/main` の suppressions と **(file × rule) 粒度**で比較し、新規エントリ・count 増 = exit 1／削除・減少のみ PASS。AM-14 の縮小単調と同族。
- CI の shallow checkout（fetch-depth 1）では `origin/main` が無いため、`--depth=1` fetch の fallback を実装。`RATCHET_BASELINE_REF` で ref 上書き可。
- 組込は `npm run check`（type-check → lint → **lint:ratchet** → test → build）。**workflow ファイル・job 名 `check` は不変**（rename すると required check が外れる罠の回避〔伝聞: hub 周知〕）。
- **変異注入で赤を実証〔実測〕**（C1 と同じ受入基準）: ①新規エントリ注入 → exit 1、②既存 count +1 → exit 1、revert → exit 0。
- **shallow fallback も実測**: `git clone --depth=1`（origin/main 無し）で exit 0。実 CI ログでも `OK: 522 → 522 件 (baseline: origin/main)` の稼働を確認〔実測: run 29596351442〕。
- current.md への反映は PR #184（merged: ea6c9c3・docs のみ）。

---

## admin 生成物の再ビルド同期（#185 / PR #186・merged: b7eb1ea）
- #182 の受入で `npm run check` を回したところ、**`npm run build` が `public_html/admin/app.js` を書き換える**ことを発見〔実測: `git status` dirty〕。差分は `__toESM(x)` → `__toESM(x, 1)` の CJS interop フラグ 573 行。
- 根因は C1 で `frontend/package.json` に `"type": "module"` を追加したこと。esbuild の出力が変わり、**main にコミット済みの生成物が現行ビルドと乖離**していた（C1 由来の既存ドリフト・当時は再ビルドしなかったため気づかず）。
- スコープ外だったため PR #183 では生成物を restore して不変を維持し、hub へ裁定依頼 → **「(a) 再ビルド commit を別 Issue で GO」**〔伝聞: hub 裁定・「嘘をつく成果物」〕。
- 対応は機械 diff のみ（ソース変更ゼロ・widget.js / fonts / app.css 不変）。**冪等性を実測**: 2 回の独立ビルドがバイト同一（stash 比較 diff 0 行）。マージ後の main で再ビルド → `git status` クリーン〔実測〕。

---

## ★教訓
- **「凍結」は増量ゲートとセットで初めて凍結**: bulk-suppressions 導入（C1）だけでは 3 週間で 518→522 に静かに増えた〔実測〕。shrink-only は宣言ではなく機械検証で担保する。
- **生成物を commit する構成は「クリーンツリーで build → status クリーン」の検証が要る**: CI は生成物の鮮度を見ないため、`type: module` 追加のような間接的変更で乖離しても誰も気づかない。今回は別作業の受入実測が偶然検出した。
- 昨日の教訓「feature ブランチは origin/main から切る」を全 3 ブランチで適用（pull --ff-only 確認後に分岐）。CI 未起動の再発なし〔実測〕。

---

## 📊 本日の数字
- merged PR: **3**（#183 ratchet・#184 current.md・#186 生成物同期）／ closed Issue: **2**（#182・#185）
- ratchet が守る凍結在庫: **522 件 / 33 files / 139 entries**〔実測: スクリプト出力〕
- 変異注入: 2 種とも赤を確認〔実測〕／ shallow fallback: 実測 1 回＋実 CI ログ確認
- 相談で止めた回数: 1（生成物乖離のスコープ外判断 → hub 裁定 (a) を受けて着手）

---

## 申し送り
- ratchet のベースラインは `origin/main` **tip**。main 側で suppressions が減った後の古いブランチは、追加していなくても FAIL し得る（rebase を強制する仕様・縮小単調の意図どおり）。
- 記事ネタ 2 件（「ゲートを守るゲート」「嘘をつくコミット済み生成物」）を articles リナへ relay 済み（standing 指示〔伝聞: hub 同報〕に基づく・一次資料は PR #183 / #186）。
- suppressions の実削減・inline 838 件は引き続き A2 領域（本日の変更はゲートのみ）。
