# 2026-07-17

フロントリファクタ phase の **C1（ESLint ゲート導入）** を完遂し、フリート唯一だった concierge の ESLint 欠落を解消した（ESLint ゲート 13/13 達成〔伝聞: fleet-tooling 集計〕）。既存 522 違反〔実測〕を ESLint 公式の native bulk-suppressions で凍結し、新規のみ error＝shrink-only にする方式を fleet が pilot として sanction。Wave G（stylelint）は concierge 固有の事情で park。指揮は統合リナ（hub）、裁定は hub→fleet-tooling ルート。

---

## ESLint ゲート導入（#178 / PR #179・merged）
- nene2-standards 共有 config を採用（`base + fsd + api + i18n + testing`）。concierge は唯一の esbuild・非FSD リポ。
- **styling 段は意図的に除外**: concierge は非Tailwind。`better-tailwindcss` は Tailwind entry 必須で throw／偽陽性洪水になる（hub 裁定 A案・fleet-tooling sanction〔伝聞〕）。
- 既存 **522 違反〔実測: `npx eslint .`〕** を `eslint-suppressions.json`（33 files〔実測〕）に凍結。CI は `eslint`（`--suppress-all` 無し）＝既存 suppress・新規 error（G-7 改竄防止）。
- CI は payout 同形で `npm run check` 経由に集約（type-check → lint → test → build）。
- **変異注入で赤を実証〔実測〕**: src に新規違反 1 件 → `eslint` exit 1（赤）／revert → exit 0（緑）。空虚合格でない証明。
- 残: 445 件のロケール JP 誤検知（canonical 免除 glob が FSD パス束縛）は per-repo messages-glob override で免除。registries transition kind への登録は **fleet 側**（kind 設計中・当面は config コメントが暫定台帳〔伝聞: hub〕）。

---

## 調査と相談（止まり方の判断）
- 初期実測で **963 違反〔実測〕**。うち **445 は locale 定義ファイルの JP 文字列〔実測〕**（canonical の JP 免除が FSD パス `src/shared/i18n/messages/**` 固定で、concierge の `src/admin/i18n/messages/**` に mismatch）。免除を実パスへ写して真負債 **518〔実測〕** に。
- フリートに eslint baseline 機構が無い（12 緑リポは全て `--max-warnings 0` の clean-built〔実測: `find` で suppressions ファイル 0 件〕）ことを確認。hub の「ad-hoc 発明禁止・無ければ止めて報告」ガードレールに該当したため、実装前に**止めて hub に相談**。
- 結果、fleet-tooling が ESLint 公式 native bulk-suppressions を「既存負債リポの公式凍結パターン」として sanction・concierge を pilot に〔伝聞: fleet-tooling〕。invoice の stylelint park と同型の判断ルート。

---

## ★教訓: base 起点は origin/main で切る
- 初回 PR が CI 未起動 → 調べると PR が CONFLICTING。`pull_request` CI は head+base のマージ ref 上で走るため、コンフリクトがあると起動しない。
- 根因は **stale なローカル `main`（05d6761）から分岐**したこと（origin/main は e4259b1・9 commits 先）。work order の「origin/main で再計測」に照らせば origin/main 起点にすべきだった。
- 是正: origin/main へ載せ替え、suppressions を更新 src で再生成（518→**522**）、force-push で解消。**feature ブランチは常に `origin/main` から切る**。

---

## governance 修正（#180・merged・self-merge）
- `docs/todo/current.md` に C1 完了を反映。
- CLAUDE.md の「main 直 push 例外（current.md）」の記述を実態へ是正: **main は保護ブランチで直 push は hook 拒否**〔実測: `remote rejected ... protected branch hook declined`〕。current.md 含め**常に PR 経由**へ。doc と infra の食い違いを解消。

---

## Wave G（stylelint）= park 維持
- concierge は **ソースの `.css` が 0 本〔実測: `find`〕**。唯一の `.css` は git 追跡された esbuild 生成物 `public_html/admin/app.css`（670 行）。監査の「CSS 218」はこの生成物由来〔伝聞: 2026-07-15 fleet-css 監査〕。
- 生成物の凍結は shrink-only ラチェット（手書きソースの単調減少）を壊し、実負債（CSS-in-JS＋inline）を測れない。→ fleet が park 裁定〔伝聞〕。core stylelint 違反の grandfather 経路が現ツールに無い＋Q1 未裁定・fleet#64 の kind レーン出荷待ち。
- concierge の実 CSS 負債（inline 838 等）は fleet#56（未計測層・concierge 優先）と A2 再建の領域。

---

## 📊 本日の数字
- 凍結した ESLint 違反: **522**〔実測〕 / 対象 33 files〔実測〕
- merged PR: **2**（#179 ESLint ゲート・#180 governance）／ closed Issue: **1**（#178）
- フリート ESLint ゲート: **13/13**〔伝聞: fleet-tooling 集計〕
- 相談で止めた回数: 1（eslint baseline 機構欠如のガードレール発動 → hub→fleet 裁定）

---

## 申し送り
- **Wave G stylelint** 再開は fleet#64 出荷後・hub 中継待ち。
- 445 免除の **registries transition kind 登録は fleet 側**（concierge の追加 PR 不要〔伝聞: hub〕）。A2 で canonical 免除へ戻す。
- `app.css` が git 追跡された生成物である件は **A2 の観察事項**として設計書へ〔伝聞: fleet 裁定〕。
- `docs/daily/` 規約移行（旧 `2026-05-28.md` の履歴保持・CLAUDE.md 1 行参照の追加）は手空きの小 PR で。
